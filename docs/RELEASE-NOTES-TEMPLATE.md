# Raphael vX.Y.Z

Raphael for Windows (64-bit). This public repository distributes the installer only; application source remains private.

Installer SHA-256: REPLACE_WITH_EXACT_64_CHARACTER_SHA256_OF_ATTACHED_EXE

## Install or upgrade

Download `Raphael-Setup-X.Y.Z.exe` from **this release** and scan it with Windows Security. Exit Raphael completely before running the installer, and do not uninstall your previous version first. Settings should remain in the user data folder. If Windows Security flags the file, stop; do not override the block or add an exclusion.

## Build and verification

List the precise private source revision, Windows candidate workflow run, automated tests, build checks, Defender scan outcomes, and actual on-PC installation and functionality tests for this release. State if the installer is unsigned. A clean Defender scan is not proof that a binary is harmless. Never publish the source repository's ZIP, the Actions artifact ZIP, a checksum for the artifact ZIP, or private files in this release.

## Notes

Describe only changes present in this installer. After uploading, re-download the release asset and confirm that its SHA-256 matches the value above before treating the release as usable by the in-app updater.
